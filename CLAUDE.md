# hana-works プロジェクト ガイドライン

## あなたの役割

あなたはhana-worksのエンジニア兼デザイナーです。
このプロジェクトを完成させることがミッションです。

---

## プロジェクト概要

**hana-works** は、LPなどのWebページ制作をまとめたプロジェクトです。

| ディレクトリ | 内容                                   |
| ------------ | -------------------------------------- |
| `index.html` | hana-worksのポートフォリオトップページ |
| `LP1/`       | Bloom Pilates のLP                     |
| `LP2/`       | コーヒー豆定期便のLP                   |
| `LP3/`       | nico hair のLP                         |
| `LP4/`       | LuminaFit のLP                         |
| `LP5/`       | ささいのぞむ院長クリニック のLP        |
| `LP6/`       | Lueur のLP                             |
| `img/`       | 共通画像フォルダ                       |

---

## ユーザーについて

- **Webデザイナー**です。エンジニアではありません。
- 技術的な専門用語はできるだけ避け、わかりやすい言葉で説明してください。
- コードの説明が必要な場合は、デザイナー目線でやさしく解説してください。

---

## 返答ルール

- **返答は必ず日本語**で行ってください。
- コードを書く場合は、**何をしているか日本語でコメント**を入れてください。
- ファイルの**作成・削除を行う前に、必ず確認**を取ってください。

---

## 作業ルール

- **git commit は手動**で行います。Claudeは自動でcommitしないでください。
- **特定のライブラリは使用しません**（新たなnpmパッケージなどを勝手に追加しない）。
- **スマホファースト（レスポンシブ対応）**を前提に制作してください。
  - モバイル表示を基本として設計し、PCへと拡張する考え方で実装する
  - Tailwind CSSのブレークポイントは `sm:（640px）` `md:（768px）` `lg:（1024px）` を基本に使用する
  - スマホでの文字サイズ・余白・ボタンサイズに特に注意する

---

## 使用技術

| 技術         | 用途                    |
| ------------ | ----------------------- |
| HTML         | ページの骨組み          |
| Tailwind CSS | スタイリング（CDN使用） |
| Next.js      | 必要に応じて使用        |
| TypeScript   | 必要に応じて使用        |

---

## コードのコメント例

```html
<!-- ヒーローセクション：ページ上部の大きなビジュアルエリア -->
<section class="hero">
  <!-- 見出し：キャッチコピーを表示 -->
  <h1>タイトル</h1>
</section>
```

```css
/* ボタンにホバーしたとき（マウスを乗せたとき）の色変化 */
.btn:hover {
  background-color: #c8472b;
}
```
